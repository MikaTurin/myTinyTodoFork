<?php
//die($_SERVER['REMOTE_ADDR']);

/*$ips = array('192.168.0.1');

if (!isset($_GET['loadLists']) && !isset($_GET['loadTasks']))
{
    if (!in_array($_SERVER['REMOTE_ADDR'], $ips)) die('IP not allowed!');
}*/

